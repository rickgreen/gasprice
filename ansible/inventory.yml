---
# Ansible inventory for the gasprice VPS.
# After `terraform apply`, fill in the server IP from `terraform output server_ip`.

all:
  hosts:
    gasprice:
      ansible_host: ""  # ← Fill in after terraform apply
      ansible_user: root
      ansible_ssh_private_key_file: ~/.ssh/gasprice_deploy
